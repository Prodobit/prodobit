{
  "name": "@prodobit/database",
  "version": "0.1.1",
  "description": "Database schema and migrations with Drizzle ORM",
  "type": "module",
  "main": "./dist/src/index.js",
  "types": "./dist/src/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "import": "./dist/src/index.js"
    }
  },
  "scripts": {
    "build": "swc src -d dist --copy-files && tsc --emitDeclarationOnly",
    "dev": "swc src -d dist --copy-files --watch",
    "typecheck": "tsc --noEmit",
    "db:generate": "pnpm build && drizzle-kit generate",
    "db:migrate": "pnpm build && drizzle-kit migrate",
    "db:studio": "pnpm build && drizzle-kit studio",
    "db:push": "pnpm build && drizzle-kit push"
  },
  "dependencies": {
    "decimal.js": "^10.6.0",
    "drizzle-orm": "^0.37.0",
    "postgres": "^3.4.5"
  },
  "devDependencies": {
    "@swc/cli": "^0.7.8",
    "@swc/core": "^1.13.3",
    "@types/pg": "^8.11.10",
    "drizzle-kit": "^0.31.4",
    "typescript": "^5.6.3"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/prodobit/prodobit.git",
    "directory": "packages/database"
  }
}
